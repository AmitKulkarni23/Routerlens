# Server-side env vars for Vercel serverless functions under frontend/api/.
# Do NOT use VITE_ prefix — that would inline values into the browser bundle.
# Copy to .env and fill in.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
